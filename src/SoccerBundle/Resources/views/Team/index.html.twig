{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Team list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Logo_uri</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('soccer_team_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td><a href="{{ path('soccer_team_player_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td><img src="{{ asset('uploads/'~entity.logouri) }}" height="150px" alt="img" /></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('soccer_team_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('soccer_team_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('soccer_team_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
